dist: xenial
language: python
python: "2.7"

stages:
  - test

matrix:
    - stage: test
      before_install:
        - wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O ~/miniconda.sh

    - stage: test
      os: osx
      # Selection of Python version is not available via language: on OS X workers, see https://github.com/travis-ci/travis-ci/issues/2312
      language: generic
      before_install:
        - wget https://repo.anaconda.com/miniconda/Miniconda3-latest-MacOSX-x86_64.sh -O ~/miniconda.sh

install:
  - bash ~/miniconda.sh -b
  - python2 -m pip list
  - python2 -m pip install --upgrade virtualenv
  - virtualenv -vvv --with-traceback -p python2 .venv
  - . .venv/bin/activate

script:
  - cat ~/miniconda3/bin/conda
  - env
  - ~/miniconda3/bin/conda --version
  - ~/miniconda3/bin/python -c 'import sys; print(sys.path); print(sys.prefix)'
  - python2 -c "import subprocess; subprocess.check_call(['$HOME/miniconda3/bin/conda', '--version'])"
  - deactivate
  - python2 -c "import subprocess; subprocess.check_call(['$HOME/miniconda3/bin/conda', '--version'])"
